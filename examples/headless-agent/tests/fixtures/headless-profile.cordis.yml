- id: agent-default-model
  config:
    provider: cli-mock
    model: cli-mock

- id: tools
  config:
    mode: native
    lazyLoading:
      enabled: on

- insert:
    - id: cli-mock-llm
      name: './snapshot-fixtures/cli-mock-llm.ts'
