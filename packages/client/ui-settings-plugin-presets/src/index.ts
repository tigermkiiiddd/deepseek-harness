/** Host loader entry for the by-preset browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the by-preset Plugins tab. */
export function apply(): void {}
