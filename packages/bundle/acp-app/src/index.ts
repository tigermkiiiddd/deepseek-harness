/**
 * Placeholder entry point for the `@deepseek-ai/dsh-acp-app` bundle package.
 *
 * The bundle's runtime value is its `cordis.patch.yml` patch layer, which is
 * consumed by the profile launcher; this module exists only to satisfy Node
 * package resolution.
 *
 * @module @deepseek-ai/dsh-acp-app
 */

export {}
